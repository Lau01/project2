
<% if @current_user == @game.guesser_id %>
  <%= render partial: 'readyguesser' %>
<% elsif @current_user == @game.drawer_id %>
  <%= render partial: 'readydrawer' %>
<% end %>


<%= link_to 'Ready', play_path(@game) %>
